A simple application to read, annotate and share books stored in Fluidinfo.

IMPORTANT INSTRUCTIONS:

This project uses submodules for the fluidinfo.js javascript library. Once you have cloned this repos you need to issue the following commands:

  $ git submodule init
  $ git submodule update

You'll then find the fluidinfo.js source in the js/fi directory (relative to the project root).
